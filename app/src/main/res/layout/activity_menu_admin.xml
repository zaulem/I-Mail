<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="16dp">

    <!-- Lista de artículos en la parte superior -->
    <TextView
        android:id="@+id/tvTituloLista"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="📋 Artículos registrados"
        android:textSize="18sp"
        android:textStyle="bold"
        android:textColor="#000000"
        android:layout_marginBottom="8dp" />

    <ListView
        android:id="@+id/listViewArticulos"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:divider="@android:color/transparent"
        android:dividerHeight="8dp" />

    <!-- Botones debajo de la lista -->
    <Button
        android:id="@+id/btnRegistrarArticulo"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Registrar artículo nuevo"
        android:minHeight="48dp"
        android:layout_marginTop="12dp" />

    <Button
        android:id="@+id/btnExportarCSV"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Exportar inventario a CSV"
        android:minHeight="48dp"
        android:layout_marginTop="12dp" />

    <Button
        android:id="@+id/btnCerrarSesion"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="🔓 Cerrar sesión"
        android:minHeight="48dp"
        android:layout_marginTop="24dp" />
</LinearLayout>
